<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center_horizontal">

    <!-- Lab Title -->
    <TextView
        android:id="@+id/labTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Research Lab"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="20dp"/>

    <!-- Lab Image -->
    <ImageView
        android:id="@+id/labImage"
        android:layout_width="250dp"
        android:layout_height="180dp"
        android:scaleType="centerCrop"
        android:src="@drawable/img"
        android:layout_marginBottom="20dp"/>

    <TextView
        android:id="@+id/labDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:text="The Research Lab is used to perform practicals of Project I/II, Data Structures, Java Programming, Operating System and Domain specific mini project. The software's that are installed includes Ubuntu OS, Atom editor, Brackets editor, PyCharm editor, MySQL, MongoDB, GCC, and Open JDK packages."
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="40dp"/>

    <!-- Back Button -->
    <Button
        android:id="@+id/btnBack"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Back"/>

</LinearLayout>

